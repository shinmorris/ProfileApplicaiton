= form_for @group do |f|
  = f.label :text, class: "group_input", placeholder: "グループ名を作成してください"
  = f.text_field :name
  = f.collection_check_boxes :user_ids, User.all, :id, :name
  = f.submit "Save", class: "chat-group-form__action-btn"
